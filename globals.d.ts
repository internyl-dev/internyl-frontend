export = {};

declare global {
    var readInternshipData: string; // the collection name in Firestore to read the internship data from
    var readUserData: string; // the collection name in Firestore to read the user data from
}